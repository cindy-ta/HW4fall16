%h1 Create New User

= form_tag users_path do

  = button_to 'Sign up/Login', new_user_path
  
  = label :user, :user_id 'User-ID'
  = text_field :user, :user_id, 'User-ID'
  
  = label :user, :email 'Email'
  = text_field :user, :email, 'Email'
  
  = submit_tag 'Create My Account'